{
    "name": "root",
    "private": true,
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "models:update": "cd packages/twc-models && yarn version --patch --no-git-tag-version && yarn build && cd .. && yarn",
        "server:up": "cd packages/twc-server && yarn start:prod"
    },
    "devDependencies": {
        "@types/axios": "^0.14.0",
        "lerna": "^5.5.1"
    },
    "version": "0.0.3",
    "dependencies": {
        "axios": "^0.27.2",
        "mongoose": "^6.6.1"
    }
}
